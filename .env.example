# MITMIT — mis en réserve (septembre 2026) : AUCUNE variable d'environnement n'est lue.
#
# Tous les connecteurs ont été débranchés : Clerk, Anthropic, Supabase, Google OAuth / Gmail,
# Shopify, OpenRouteService, La Poste / Colissimo, Qonto, et le domaine du bon de commande.
# Le dossier api/ (fonctions serverless) a été retiré. L'application tourne en « local pur » :
# données dans le navigateur, sans compte, sans synchronisation, sans service payant.
#
# L'outil actif est désormais l'application B2B PEN'UP 3D (https://penup-3d-b2b.vercel.app/).
#
# Pour retrouver la version branchée (les 21 variables et où les créer, les 12 fonctions api/) :
#   git checkout avant-debranchement
